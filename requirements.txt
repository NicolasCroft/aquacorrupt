# Core (feature extraction step)
torch>=2.1
torchvision>=0.16
timm>=1.0.0
# Analysis (probe + plots) -- also enough to run corruptions standalone
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pillow>=9.5
matplotlib>=3.7
# Optional: dataset download via Kaggle API
# kaggle>=1.6
